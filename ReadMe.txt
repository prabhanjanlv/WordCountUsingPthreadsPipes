Use any C Compiler to execute the Program pwordcount

complile the program and while executing the output file, pass a filename as input parameter

The input file has to be in the same directory as the source files

This program opens the mentioned input file counts the number of characters using pthreads concept and closes the input file
writes the output in console.